# 支付功能集成设计文档

## 1. 设计目标

本设计旨在为直播平台添加真实的支付功能，支持多种支付方式，确保支付流程安全、可靠、便捷。

## 2. 支付架构

### 2.1 整体架构

```
前端 → 后端API → 支付服务 → 第三方支付平台
```

### 2.2 核心模块

1. **支付服务**：负责处理支付相关的业务逻辑
2. **支付记录**：记录所有支付交易
3. **回调处理**：处理第三方支付平台的异步回调
4. **支付配置**：管理支付方式的配置信息

## 3. 数据模型

### 3.1 支付记录（Payment）

| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | bigint | 主键ID |
| orderId | varchar(50) | 订单ID |
| userId | bigint | 用户ID |
| amount | decimal(10,2) | 支付金额 |
| paymentMethod | varchar(50) | 支付方式 |
| paymentStatus | int | 支付状态（0-待支付，1-已支付，2-支付失败） |
| transactionId | varchar(100) | 第三方交易ID |
| callbackData | text | 回调数据 |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |
| payTime | datetime | 支付时间 |

### 3.2 支付配置（PaymentConfig）

| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | bigint | 主键ID |
| configKey | varchar(50) | 配置键 |
| configValue | text | 配置值 |
| paymentMethod | varchar(50) | 支付方式 |
| status | int | 状态（0-禁用，1-启用） |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

## 4. 支付流程

### 4.1 充值支付流程

1. **用户发起充值**：用户在前端选择充值金额和支付方式
2. **创建充值订单**：后端生成充值订单和支付订单
3. **调用支付接口**：后端调用第三方支付平台的支付接口
4. **用户完成支付**：用户在第三方平台完成支付
5. **回调处理**：第三方支付平台异步回调通知支付结果
6. **更新订单状态**：后端更新支付状态和用户余额

### 4.2 支付状态流转

```
待支付 → 支付中 → 已支付/支付失败
```

## 5. 接口设计

### 5.1 后端API

| 接口路径 | 方法 | 描述 |
|---------|------|------|
| /api/payment/create | POST | 创建支付订单 |
| /api/payment/callback/{method} | POST | 支付回调处理 |
| /api/payment/query/{orderId} | GET | 查询支付状态 |
| /api/payment/list | GET | 获取支付记录 |

### 5.2 前端接口

| 接口路径 | 方法 | 描述 |
|---------|------|------|
| /payment/create | POST | 创建支付订单 |
| /payment/query | GET | 查询支付状态 |
| /payment/list | GET | 获取支付记录 |

## 6. 安全设计

### 6.1 签名验证
- 使用密钥对请求和响应进行签名验证
- 防止请求被篡改

### 6.2 数据加密
- 敏感信息（如商户密钥）加密存储
- 传输过程中使用HTTPS

### 6.3 防重放攻击
- 使用订单号和时间戳防止重复请求
- 实现幂等性设计

### 6.4 日志记录
- 详细记录支付流程的关键操作
- 便于问题排查和审计

## 7. 常见支付方式集成

### 7.1 支付宝支付

#### 7.1.1 准备工作
1. 注册支付宝开发者账号
2. 创建应用并获取AppID
3. 配置密钥（RSA2）
4. 配置回调地址

#### 7.1.2 集成步骤
1. 添加支付宝SDK依赖
2. 配置支付宝参数
3. 实现支付接口调用
4. 实现回调处理

### 7.2 微信支付

#### 7.2.1 准备工作
1. 注册微信支付商户号
2. 获取AppID和商户密钥
3. 配置回调地址

#### 7.2.2 集成步骤
1. 添加微信支付SDK依赖
2. 配置微信支付参数
3. 实现支付接口调用
4. 实现回调处理

### 7.3 银行卡支付

#### 7.3.1 准备工作
1. 对接银行或第三方支付机构
2. 获取商户号和密钥
3. 配置回调地址

#### 7.3.2 集成步骤
1. 添加银行卡支付SDK依赖
2. 配置银行卡支付参数
3. 实现支付接口调用
4. 实现回调处理

## 8. 错误处理

### 8.1 常见错误

| 错误码 | 错误描述 | 处理方式 |
|-------|---------|---------|
| 400 | 参数错误 | 返回错误信息给前端 |
| 401 | 未授权 | 返回未授权错误 |
| 404 | 订单不存在 | 返回订单不存在错误 |
| 500 | 系统错误 | 记录错误日志并返回系统错误 |
| 503 | 支付服务不可用 | 返回服务不可用错误 |

### 8.2 异常处理
- 使用全局异常处理器捕获和处理异常
- 对支付相关的异常进行专门处理

## 9. 部署与测试

### 9.1 部署
1. 配置支付参数（商户ID、密钥等）
2. 部署应用到生产环境
3. 测试支付流程

### 9.2 测试
1. **单元测试**：测试支付服务的核心功能
2. **集成测试**：测试支付流程的完整性
3. **压力测试**：测试支付系统的性能
4. **安全测试**：测试支付系统的安全性

## 10. 集成计划

### 10.1 阶段一：基础架构
- 创建支付相关的数据模型
- 实现支付服务的核心功能
- 实现支付接口

### 10.2 阶段二：支付宝集成
- 集成支付宝支付SDK
- 实现支付宝支付接口
- 测试支付宝支付流程

### 10.3 阶段三：微信支付集成
- 集成微信支付SDK
- 实现微信支付接口
- 测试微信支付流程

### 10.4 阶段四：银行卡支付集成
- 集成银行卡支付SDK
- 实现银行卡支付接口
- 测试银行卡支付流程

### 10.5 阶段五：优化与测试
- 优化支付流程
- 完善错误处理
- 进行全面测试

## 11. 技术栈

### 11.1 后端
- Spring Boot 3
- MyBatis Plus
- 支付宝SDK
- 微信支付SDK
- MySQL

### 11.2 前端
- Vue 3
- TypeScript
- Ant Design Vue
- Axios

### 11.3 部署
- Docker
- Docker Compose
- Nginx

## 12. 风险评估

### 12.1 技术风险
- 第三方支付平台接口变更
- 支付安全漏洞
- 系统性能问题

### 12.2 业务风险
- 支付失败导致用户体验差
- 资金安全问题
- 合规性问题

### 12.3 应对措施
- 定期更新第三方支付SDK
- 加强安全防护措施
- 建立监控和预警机制
- 遵循相关法律法规

## 13. 结论

本设计方案通过合理的架构设计和安全措施，实现了多种支付方式的集成，为用户提供了安全、便捷的支付体验。同时，通过详细的集成步骤和测试计划，确保了支付功能的可靠性和稳定性。