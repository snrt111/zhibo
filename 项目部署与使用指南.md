# 直播平台项目部署与使用指南

## 1. 项目概述

本项目是一个功能完整的直播平台，支持直播推流、观看、弹幕、礼物、评论、AI图片生成等功能。

### 已实现功能

| 模块 | 功能 |
|------|------|
| 用户系统 | 注册、登录（密码/验证码/第三方）、个人中心 |
| 直播系统 | 推流、观看、管理、分类、搜索 |
| 互动系统 | 弹幕、礼物、关注、评论 |
| 支付系统 | 充值、提现、余额管理 |
| 管理系统 | 用户管理、直播管理、内容审核、数据分析 |
| AI工具 | 图片生成、压缩、历史记录 |
| 通知系统 | 开播提醒、实时推送 |
| 观看历史 | 自动记录、继续观看 |

### 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端 | Vue 3 + TypeScript | 3.5.x / 5.9.x |
| 前端UI | Ant Design Vue | 4.x |
| 构建工具 | Vite | 7.x |
| 后端 | Spring Boot | 3.1.x |
| ORM | MyBatis Plus | 3.5.x |
| 数据库 | MySQL | 8.0.33 |
| 缓存 | Redis | 7.0.15-alpine |
| 消息队列 | RabbitMQ | 3.12.14-management-alpine |
| 搜索引擎 | Elasticsearch | 8.13.0 |
| 对象存储 | MinIO | latest |
| 流媒体 | Nginx-RTMP + FFmpeg | latest |
| 容器化 | Docker + Docker Compose | - |

---

## 2. 环境要求

### 本地开发环境

| 软件 | 版本要求 |
|------|----------|
| JDK | 17+ |
| Node.js | 18+ |
| Maven | 3.8+ |
| Docker | 20.10+ |
| Docker Compose | 2.0+ |
| FFmpeg | 4.0+ |

### 端口占用说明

| 服务 | 端口 | 说明 |
|------|------|------|
| MySQL | 3307 | 外部访问端口（容器内3306） |
| Redis | 6379 | 缓存服务 |
| RabbitMQ | 5672 / 15672 | 消息队列 / 管理界面 |
| Elasticsearch | 9200 / 9300 | 搜索引擎 |
| MinIO | 9000 / 9001 | 对象存储 / 管理界面 |
| 后端服务 | 8081 | API服务 |
| 前端服务 | 80 | Web服务 |
| RTMP服务 | 1935 | 推流端口 |
| HLS服务 | 8080 | 拉流端口 |

---

## 3. 快速开始

### 3.1 一键部署

```powershell
# 1. 构建镜像（后端 + 前端）
.\build-all.ps1

# 2. 启动所有服务
docker-compose up -d

# 3. 访问应用
# 前端界面: http://localhost
# 后端API:  http://localhost:8081/api
```

### 3.2 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | 123456 |
| 普通用户 | user1 | 123456 |
| 普通用户 | user2 | 123456 |

---

## 4. Docker部署

### 4.1 构建项目镜像

**方式一：一键构建（推荐）**

```powershell
.\build-all.ps1
```

**方式二：分别构建**

```powershell
# 构建后端
.\build-backend.ps1

# 构建前端
.\build-frontend.ps1
```

**方式三：手动构建**

```bash
# 后端
cd backend
mvn clean package -DskipTests
docker build -t zhibo-backend .
cd ..

# 前端
cd frontend
npm install
npm run build
docker build -t zhibo-frontend .
cd ..
```

### 4.2 启动服务

```bash
# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f backend
docker-compose logs -f frontend
```

### 4.3 停止服务

```bash
# 停止服务但保留数据
docker-compose stop

# 停止并删除容器（保留数据卷）
docker-compose down

# 停止并删除所有数据（包括数据卷）
docker-compose down -v
```

### 4.4 构建脚本说明

**build-all.ps1 选项：**

| 参数 | 说明 |
|------|------|
| `--no-backend` | 跳过后端构建 |
| `--no-frontend` | 跳过前端构建 |
| `--with-tests` | 运行测试 |
| `--no-cache` | 不使用Docker缓存 |
| `-h, --help` | 显示帮助信息 |

### 4.5 服务编排说明

**docker-compose.yml** 定义了以下服务：

| 服务 | 镜像 | 说明 |
|------|------|------|
| mysql | mysql:8.0.33 | MySQL数据库 |
| redis | redis:7.0.15-alpine | Redis缓存 |
| rabbitmq | rabbitmq:3.12.14-management-alpine | RabbitMQ消息队列 |
| elasticsearch | elasticsearch:8.13.0 | Elasticsearch搜索引擎 |
| minio | minio/minio:latest | MinIO对象存储 |
| backend | zhibo-backend | Spring Boot后端服务 |
| frontend | zhibo-frontend | Vue前端服务 |
| nginx-rtmp | alfg/nginx-rtmp | Nginx-RTMP流媒体服务 |

**服务依赖关系：**

```
mysql ────────┐
redis ────────┤
rabbitmq ─────┼──> backend ──┬──> frontend
elasticsearch─┤              │
minio ─────────┘              │
nginx-rtmp ──────────────────┘
```

### 4.6 数据持久化

| 服务 | 数据卷名称 | 用途 |
|------|------------|------|
| MySQL | mysql-data | 数据库数据 |
| Redis | redis-data | 缓存数据 |
| RabbitMQ | rabbitmq-data | 消息队列数据 |
| Elasticsearch | elasticsearch-data | 搜索索引 |
| HLS | hls-data | HLS流文件 |
| MinIO | minio-data | 对象存储数据 |

---

## 5. 本地开发

### 5.1 启动基础设施

```powershell
# 启动MySQL、Redis、RabbitMQ、Elasticsearch、MinIO、Nginx-RTMP
docker-compose up -d mysql redis rabbitmq elasticsearch minio nginx-rtmp
```

### 5.2 启动后端

```powershell
cd backend

# 方式一：使用Maven插件
mvn spring-boot:run -Dspring.profiles.active=dev

# 方式二：先打包再运行
mvn clean package -DskipTests
java -jar target/backend-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev
```

### 5.3 启动前端

```powershell
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

### 5.4 开发环境访问

| 服务 | 地址 |
|------|------|
| 前端开发服务器 | http://localhost:5173 |
| 后端API | http://localhost:8081/api |

---

## 6. 配置说明

### 6.1 数据库配置

**凭据信息：**
- 用户名: `root`
- 密码: `123456`
- 数据库: `zhibo`

**本地连接：**
```bash
mysql -h localhost -P 3306 -u root -p123456
```

### 6.2 后端配置

**配置文件**: `backend/src/main/resources/application.yml`

**主要配置项：**

```yaml
# 数据库连接
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zhibo
    username: root
    password: 123456

# Redis
  data:
    redis:
      host: localhost
      port: 6379

# RabbitMQ
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# Elasticsearch
  elasticsearch:
    uris: http://localhost:9200

# JWT
jwt:
  secret: zhibo-secret-key-2026
  expiration: 86400000

# 文件上传
upload:
  path: /app/uploads
  url-prefix: http://localhost:8081/api/uploads
  use-minio: false

# MinIO对象存储
minio:
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin123
  bucketName: zhibo-files

# 流媒体
stream:
  rtmp:
    server: rtmp://localhost:1935/live
  hls:
    server: http://localhost:8080/hls

# 阿里云短信（可选）
sms:
  aliyun:
    enabled: false
    access-key-id: your-access-key
    access-key-secret: your-secret
    region-id: cn-hangzhou
    sign-name: 您的签名
    template-code: SMS_123456789
```

**环境变量覆盖：**

| 环境变量 | 说明 |
|----------|------|
| `SPRING_DATASOURCE_URL` | 数据库连接URL |
| `SPRING_DATASOURCE_USERNAME` | 数据库用户名 |
| `SPRING_DATASOURCE_PASSWORD` | 数据库密码 |
| `SPRING_REDIS_HOST` | Redis主机 |
| `SPRING_RABBITMQ_HOST` | RabbitMQ主机 |
| `SPRING_ELASTICSEARCH_URIS` | Elasticsearch地址 |
| `JWT_SECRET` | JWT密钥 |
| `UPLOAD_USE_MINIO` | 是否使用MinIO存储 |
| `MINIO_ENDPOINT` | MinIO服务地址 |
| `MINIO_ACCESS_KEY` | MinIO访问密钥 |
| `MINIO_SECRET_KEY` | MinIO密钥 |
| `MINIO_BUCKET_NAME` | MinIO存储桶 |
| `STREAM_RTMP_SERVER` | RTMP服务器地址 |
| `STREAM_HLS_SERVER` | HLS服务器地址 |

### 6.3 前端配置

**开发环境配置**: `frontend/.env.development`

```
VITE_API_BASE_URL=http://localhost:8081/api
VITE_WS_URL=ws://localhost:8081/ws
```

**生产环境配置**: `frontend/.env.production`

```
VITE_API_BASE_URL=/api
VITE_WS_URL=/ws
```

**Vite配置**: `frontend/vite.config.ts`

```typescript
// 开发服务器
server: {
  host: '0.0.0.0',
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:8081',
      changeOrigin: true
    }
  }
}
```

**Nginx配置**: `frontend/nginx.conf`

```nginx
# API代理
location /api/ {
    proxy_pass http://backend:8081/api/;
}

# WebSocket代理
location /ws/ {
    proxy_pass http://backend:8081/ws/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

### 6.4 流媒体配置

**Nginx-RTMP配置**: `nginx-rtmp.conf`

```nginx
rtmp {
    server {
        listen 1935;
        application live {
            live on;
            hls on;
            hls_fragment 2s;
            hls_playlist_length 6s;
            hls_path /tmp/hls;
        }
    }
}
```

---

## 7. 使用指南

### 7.1 访问服务

| 服务 | 地址 | 账号/密码 |
|------|------|-----------|
| 前端应用 | http://localhost | - |
| 后端API | http://localhost:8081/api | - |
| RabbitMQ管理 | http://localhost:15672 | guest / guest |
| MinIO管理 | http://localhost:9001 | minioadmin / minioadmin123 |
| Elasticsearch | http://localhost:9200 | 无认证 |

### 7.2 推流设置

#### FFmpeg命令行推流

```bash
# 推送测试视频流
ffmpeg -re -f lavfi -i testsrc=size=1280x720:rate=30,format=yuv420p \
       -f lavfi -i sine=frequency=440 \
       -c:v libx264 -preset veryfast -tune zerolatency \
       -g 60 -keyint_min 60 -b:v 2500k \
       -c:a aac -b:a 128k -ac 2 -ar 44100 \
       -f flv rtmp://localhost:1935/live/<stream-key>

# 推送本地视频文件
ffmpeg -re -i input.mp4 \
       -c:v libx264 -preset veryfast -tune zerolatency \
       -c:a aac -b:a 128k \
       -f flv rtmp://localhost:1935/live/<stream-key>

# 使用OBS推流
# 1. 打开OBS设置
# 2. 流服务器: rtmp://localhost:1935/live
# 3. 流密钥: <stream-key>
```

### 7.3 观看直播

#### HLS播放地址

```
http://localhost:8080/hls/<stream-key>.m3u8
```

#### 通过前端界面

1. 访问 `http://localhost`
2. 进入直播列表
3. 选择正在直播的房间观看

### 7.4 API接口示例

#### 健康检查

```bash
curl http://localhost:8081/api/health
```

#### 获取直播列表

```bash
curl http://localhost:8081/api/live/list
```

#### 用户登录

```bash
curl -X POST http://localhost:8081/api/user/login \
     -H "Content-Type: application/json" \
     -d '{"username":"admin","password":"123456"}'
```

#### 创建直播（需要Token）

```bash
curl -X POST http://localhost:8081/api/live/create \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer {token}" \
     -d '{"title":"测试直播","description":"测试","categoryId":1}'
```

---

## 8. 功能使用指南

### 8.1 直播功能

#### 创建直播

1. 登录后进入主播中心
2. 点击"创建直播"
3. 填写直播标题、描述、选择分类
4. 上传直播封面
5. 点击"创建"

#### 开始直播

1. 在主播中心找到创建的直播
2. 点击"开始直播"获取推流地址
3. 使用OBS或FFmpeg推流
4. 推流成功后，直播状态变为"直播中"

#### 观看直播

1. 在首页浏览直播列表
2. 点击进入直播间
3. 可以发送弹幕、赠送礼物、关注主播

### 8.2 礼物系统

#### 赠送礼物

1. 进入直播间
2. 点击礼物图标
3. 选择礼物
4. 点击"赠送"

#### 查看礼物记录

1. 进入个人中心
2. 点击"礼物记录"
3. 查看送出/收到的礼物

### 8.3 提现功能

#### 申请提现（主播）

1. 进入主播中心
2. 点击"提现"
3. 填写提现金额和收款账号
4. 提交申请

#### 审核提现（管理员）

1. 进入管理后台
2. 点击"提现管理"
3. 查看待审核的提现申请
4. 点击"审核通过"或"拒绝"

### 8.4 AI图片生成

#### 生成图片

1. 进入"AI工具"页面
2. 选择AI模型
3. 输入描述文字
4. 点击"生成"
5. 等待生成完成，下载图片

### 8.5 内容审核

#### 处理举报（管理员）

1. 进入管理后台
2. 点击"举报管理"
3. 查看待处理的举报
4. 选择处理方式：
   - 禁用用户
   - 结束直播
   - 删除评论/弹幕
5. 提交处理结果

---

## 9. 生产环境部署

### 9.1 环境变量配置

创建 `.env` 文件覆盖默认配置：

```env
# 数据库
MYSQL_ROOT_PASSWORD=your-secure-password
SPRING_DATASOURCE_PASSWORD=your-secure-password

# JWT
JWT_SECRET=your-production-secret-key-min-32-characters

# MinIO
MINIO_ACCESS_KEY=your-access-key
MINIO_SECRET_KEY=your-secret-key

# 阿里云短信（可选）
SMS_ALIYUN_ENABLED=true
SMS_ALIYUN_ACCESS_KEY_ID=your-access-key
SMS_ALIYUN_ACCESS_KEY_SECRET=your-secret
SMS_ALIYUN_SIGN_NAME=your-sign-name
SMS_ALIYUN_TEMPLATE_CODE=your-template-code
```

### 9.2 使用生产环境配置

```bash
# 创建生产环境配置文件 docker-compose.prod.yml
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

### 9.3 数据库备份与恢复

```bash
# 备份数据库
docker exec zhibo-mysql mysqldump -u root -p123456 zhibo > backup_$(date +%Y%m%d).sql

# 恢复数据库
docker exec -i zhibo-mysql mysql -u root -p123456 zhibo < backup.sql

# 自动备份脚本（添加到crontab）
# 0 2 * * * cd /path/to/zhibo && ./backup-db.sh
```

### 9.4 HTTPS配置

使用Nginx反向代理配置HTTPS：

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/ {
        proxy_pass http://localhost:8081/api/;
    }

    location /ws/ {
        proxy_pass http://localhost:8081/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

---

## 10. 监控与维护

### 10.1 查看资源使用

```bash
# 查看容器资源使用
docker stats

# 查看磁盘使用
docker system df
```

### 10.2 查看容器日志

```bash
# 查看所有服务日志
docker-compose logs -f --tail=100

# 查看特定服务日志
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f mysql
```

### 10.3 清理无用资源

```bash
# 清理无用镜像和容器
docker system prune -a

# 清理特定服务的日志
docker-compose logs --tail=0 > /dev/null
```

### 10.4 服务重启

```bash
# 重启特定服务
docker-compose restart backend
docker-compose restart frontend

# 重启所有服务
docker-compose restart
```

---

## 11. 常见问题

### 11.1 服务启动失败

**问题**: 服务启动后立即退出

**解决**:
```bash
# 查看日志
docker-compose logs <service-name>

# 检查端口占用
netstat -ano | findstr :8081

# 重启服务
docker-compose restart <service-name>
```

### 11.2 数据库连接失败

**问题**: 后端无法连接数据库

**解决**:
```bash
# 检查MySQL容器状态
docker-compose ps mysql

# 查看MySQL日志
docker-compose logs mysql

# 检查数据库是否初始化
docker exec -it zhibo-mysql mysql -uroot -p123456 -e "SHOW DATABASES;"
```

### 11.3 推流失败

**问题**: 无法推流到RTMP服务器

**解决**:
```bash
# 检查RTMP服务状态
docker-compose ps nginx-rtmp

# 检查端口
telnet localhost 1935

# 查看RTMP日志
docker-compose logs nginx-rtmp
```

### 11.4 前端无法访问后端

**问题**: 前端请求API返回404

**解决**:
```bash
# 检查后端服务状态
docker-compose ps backend

# 检查Nginx配置
docker exec zhibo-frontend nginx -t

# 重启前端服务
docker-compose restart frontend
```

---

## 12. 相关文档

- [需求文档](./需求文档.md) - 详细的功能需求说明
- [开发需求列表](./开发需求列表.md) - 功能开发进度
- [API接口文档](./API接口文档.md) - 接口详细说明
- [登录系统设计文档](./登录系统设计文档.md) - 登录系统设计
- [支付功能集成设计文档](./支付功能集成设计文档.md) - 支付功能设计
- [README.md](./README.md) - 项目简介
