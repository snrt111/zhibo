# 监控功能使用指南

## 1. 概述

本文档介绍了直播平台系统的监控功能，包括后端监控、前端监控以及如何使用 Prometheus 和 Grafana 进行指标可视化。

## 2. 后端监控

### 2.1 Spring Boot Actuator

系统集成了 Spring Boot Actuator，提供了丰富的监控端点：

- **健康检查**: `/api/actuator/health` - 查看系统健康状态
- **信息**: `/api/actuator/info` - 查看应用信息
- **指标**: `/api/actuator/metrics` - 查看系统指标
- **环境**: `/api/actuator/env` - 查看环境配置
- **Bean**: `/api/actuator/beans` - 查看 Spring Bean 信息
- **配置属性**: `/api/actuator/configprops` - 查看配置属性
- **HTTP 跟踪**: `/api/actuator/httptrace` - 查看 HTTP 请求跟踪
- **线程转储**: `/api/actuator/threaddump` - 查看线程转储
- **堆转储**: `/api/actuator/heapdump` - 下载堆转储文件
- **定时任务**: `/api/actuator/scheduledtasks` - 查看定时任务
- **映射**: `/api/actuator/mappings` - 查看 URL 映射

### 2.2 Micrometer 指标收集

系统使用 Micrometer 收集以下指标：

- **JVM 指标**: 内存、线程、垃圾回收等
- **系统指标**: CPU、磁盘、网络等
- **应用指标**: HTTP 请求、响应时间等
- **自定义业务指标**: 在线用户数、直播流数量、弹幕数量、礼物数量

### 2.3 自定义业务指标

系统实现了以下自定义业务指标：

- **在线用户数**: 实时在线用户数量
- **直播流数量**: 当前活跃的直播流数量
- **弹幕数量**: 系统处理的弹幕数量
- **礼物数量**: 系统处理的礼物数量

这些指标会在相应的业务操作中自动更新：

- 用户连接/断开时更新在线用户数
- 直播开始/结束时更新直播流数量
- 发送弹幕时更新弹幕数量
- 发送礼物时更新礼物数量

## 3. 前端监控

### 3.1 Sentry 错误监控

系统集成了 Sentry 用于前端错误监控，可以捕获以下内容：

- JavaScript 错误
- 资源加载错误
- API 请求错误
- 性能问题

### 3.2 监控面板

系统在管理后台提供了监控面板，展示以下内容：

- 在线用户数
- 直播流数量
- 弹幕数量
- 礼物数量
- 各项指标的趋势图表

访问路径：`/admin/monitor`

## 4. Prometheus 和 Grafana

### 4.1 Prometheus 配置

系统使用 Prometheus 作为指标存储，配置文件为 `prometheus.yml`，主要配置：

- 抓取间隔：15秒
- 抓取目标：
  - Spring Boot 应用 (`/api/actuator/prometheus`)
  - Redis
  - MySQL
  - RabbitMQ
  - Elasticsearch
  - MinIO

### 4.2 Grafana 配置

系统使用 Grafana 进行指标可视化，主要功能：

- 系统概览面板
- 应用指标面板
- 业务指标面板
- 趋势图表

Grafana 访问地址：`http://localhost:3000`

- 用户名：admin
- 密码：admin123

### 4.3 导入监控面板

1. 登录 Grafana 管理界面
2. 点击左侧菜单的 "Dashboards"
3. 点击 "Import" 按钮
4. 上传 `grafana-dashboard.json` 文件
5. 选择 Prometheus 数据源
6. 点击 "Import" 按钮完成导入

## 5. 启动监控服务

### 5.1 使用 Docker Compose 启动

在项目根目录执行以下命令启动所有服务（包括监控服务）：

```bash
docker-compose up -d
```

### 5.2 单独启动监控服务

如果只需要启动监控服务，可以执行以下命令：

```bash
docker-compose up -d prometheus grafana
```

## 6. 查看监控数据

### 6.1 查看 Prometheus 指标

访问 Prometheus 管理界面：`http://localhost:9090`

- 在 "Graph" 标签页中输入指标名称查看数据
- 常用指标：
  - `user_online_count` - 在线用户数
  - `live_stream_count` - 直播流数量
  - `danmaku_count` - 弹幕数量
  - `gift_count` - 礼物数量

### 6.2 查看 Grafana 面板

访问 Grafana 管理界面：`http://localhost:3000`

- 登录后点击左侧菜单的 "Dashboards"
- 选择 "直播平台监控面板"
- 查看各项指标的实时数据和趋势图表

## 7. 告警配置

### 7.1 Prometheus 告警规则

可以在 `prometheus.yml` 中配置告警规则，例如：

```yaml
alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']

rule_files:
  - "alerting_rules.yml"
```

### 7.2 示例告警规则

```yaml
groups:
- name: zhibo-alerts
  rules:
  - alert: HighOnlineUserCount
    expr: user_online_count > 1000
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: "高在线用户数"
      description: "在线用户数超过 1000，当前值: {{ $value }}"

  - alert: NoLiveStream
    expr: live_stream_count == 0
    for: 30m
    labels:
      severity: info
    annotations:
      summary: "无直播流"
      description: "系统当前无活跃直播流"
```

## 8. 注意事项

1. **生产环境配置**：在生产环境中，建议：
   - 配置 Sentry DSN 为真实的项目 DSN
   - 调整 Prometheus 的抓取间隔以减少资源消耗
   - 配置合适的告警规则
   - 保护监控端点的访问权限

2. **性能影响**：监控功能会对系统性能产生一定影响，建议：
   - 在生产环境中适当调整指标收集频率
   - 只收集必要的指标
   - 定期清理监控数据

3. **安全考虑**：监控端点可能包含敏感信息，建议：
   - 在生产环境中启用认证
   - 限制监控端点的访问 IP
   - 不在监控面板中展示敏感数据

## 9. 故障排查

### 9.1 监控服务无法启动

- 检查 Docker Compose 配置是否正确
- 检查端口是否被占用
- 查看容器日志：`docker-compose logs prometheus grafana`

### 9.2 指标数据不显示

- 检查 Spring Boot 应用是否正常运行
- 检查 Prometheus 配置是否正确
- 检查网络连接是否正常
- 查看 Prometheus 目标状态：`http://localhost:9090/targets`

### 9.3 前端监控无数据

- 检查 Sentry DSN 是否配置正确
- 检查浏览器控制台是否有错误
- 检查 Sentry 项目配置是否正确

## 10. 总结

本系统的监控功能提供了全面的系统状态和业务指标监控，帮助运维人员及时了解系统运行情况，发现并解决潜在问题，确保系统的稳定运行。

通过 Prometheus 和 Grafana 的集成，实现了指标的存储和可视化，为系统的运维和优化提供了有力的支持。
