# 直播平台项目配置与使用指南

## 1. 项目概述

本项目是一个功能完整的直播平台，支持直播推流、观看、弹幕、礼物等功能。

### 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端 | Vue 3 + TypeScript | 3.5.x / 5.9.x |
| 前端UI | Ant Design Vue | 4.x |
| 构建工具 | Vite | 7.x |
| 后端 | Spring Boot | 3.1.x |
| ORM | MyBatis Plus | 3.5.x |
| 数据库 | MySQL | 8.0.33 |
| 缓存 | Redis | 7.0.15-alpine |
| 消息队列 | RabbitMQ | 3.12.14-management-alpine |
| 搜索引擎 | Elasticsearch | 8.13.0 |
| 流媒体 | Nginx-RTMP + FFmpeg | latest |
| 容器化 | Docker + Docker Compose | - |

## 2. 环境要求

### 本地开发环境

| 软件 | 版本要求 |
|------|----------|
| JDK | 17+ |
| Node.js | 18+ |
| Maven | 3.8+ |
| Docker | 20.10+ |
| Docker Compose | 2.0+ |
| FFmpeg | 4.0+ |

### 端口占用说明

| 服务 | 端口 | 说明 |
|------|------|------|
| MySQL | 3307 | 外部访问端口（容器内3306） |
| Redis | 6379 | 缓存服务 |
| RabbitMQ | 5672 / 15672 | 消息队列 / 管理界面 |
| Elasticsearch | 9200 / 9300 | 搜索引擎 |
| 后端服务 | 8081 | API服务 |
| 前端服务 | 80 | Web服务 |
| RTMP服务 | 1935 | 推流端口 |
| HLS服务 | 8080 | 拉流端口 |

## 3. 快速开始

### 3.1 构建项目

```powershell
# 构建完整项目（后端 + 前端）
.\build-all.ps1

# 仅构建后端
.\build-backend.ps1

# 仅构建前端
.\build-frontend.ps1

# 构建选项
.\build-all.ps1 --no-backend     # 跳过后端
.\build-all.ps1 --no-frontend    # 跳过前端
.\build-all.ps1 --with-tests     # 运行测试
.\build-all.ps1 --no-cache       # 不使用Docker缓存
```

### 3.2 启动服务

```powershell
# 启动所有服务
.\start.ps1

# 启动特定服务
.\start.ps1 backend
.\start.ps1 mysql redis

# 查看帮助
.\start.ps1 --help
```

### 3.3 管理服务

```powershell
# 查看服务状态
docker-compose ps

# 查看服务日志
docker-compose logs -f
docker-compose logs -f backend

# 停止服务
docker-compose down

# 重启服务
docker-compose restart
```

## 4. 配置说明

### 4.1 数据库配置

**凭据信息：**
- 用户名: `root`
- 密码: `123456`
- 数据库: `zhibo`

**本地连接：**
```
mysql -h localhost -P 3307 -u root -p123456
```

### 4.2 后端配置

**配置文件**: `backend/src/main/resources/application.yml`

**主要配置项：**

```yaml
# 数据库连接
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zhibo
    username: root
    password: 123456

# Redis
  data:
    redis:
      host: localhost
      port: 6379

# RabbitMQ
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# Elasticsearch
  elasticsearch:
    uris: http://localhost:9200

# JWT
jwt:
  secret: zhibo-secret-key-2026
  expiration: 86400000

# 流媒体
stream:
  rtmp:
    server: rtmp://localhost:1935/live
  hls:
    server: http://localhost:8080/hls
```

**环境变量覆盖：**

| 环境变量 | 说明 |
|----------|------|
| `SPRING_DATASOURCE_URL` | 数据库连接URL |
| `SPRING_DATASOURCE_USERNAME` | 数据库用户名 |
| `SPRING_DATASOURCE_PASSWORD` | 数据库密码 |
| `SPRING_REDIS_HOST` | Redis主机 |
| `SPRING_RABBITMQ_HOST` | RabbitMQ主机 |
| `SPRING_ELASTICSEARCH_URIS` | Elasticsearch地址 |
| `JWT_SECRET` | JWT密钥 |
| `STREAM_RTMP_SERVER` | RTMP服务器地址 |
| `STREAM_HLS_SERVER` | HLS服务器地址 |

### 4.3 前端配置

**Vite配置**: `frontend/vite.config.ts`

```typescript
// 开发服务器
server: {
  host: '0.0.0.0',
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:8081',
      changeOrigin: true
    }
  }
}
```

**Nginx配置**: `frontend/nginx.conf`

```nginx
# API代理
location /api/ {
    proxy_pass http://backend:8081/api/;
}

# WebSocket代理
location /ws/ {
    proxy_pass http://backend:8081/ws/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

### 4.4 流媒体配置

**Nginx-RTMP配置**: `nginx-rtmp.conf`

```nginx
rtmp {
    server {
        listen 1935;
        application live {
            live on;
            hls on;
            hls_fragment 2s;
            hls_playlist_length 6s;
            hls_path /tmp/hls;
        }
    }
}
```

## 5. 使用指南

### 5.1 推流设置

#### OBS推流配置

1. 打开OBS Studio
2. 设置 -> 流 -> 服务选择"自定义"
3. 服务器: `rtmp://localhost:1935/live`
4. 推流码: `<stream-key>`

#### FFmpeg命令行推流

```bash
# 推送测试视频流
ffmpeg -re -f lavfi -i testsrc=size=1280x720:rate=30,format=yuv420p \
       -f lavfi -i sine=frequency=440 \
       -c:v libx264 -preset veryfast -tune zerolatency \
       -g 60 -keyint_min 60 -b:v 2500k \
       -c:a aac -b:a 128k -ac 2 -ar 44100 \
       -f flv rtmp://localhost:1935/live/<stream-key>
```

### 5.2 观看直播

#### HLS播放地址

```
http://localhost:8080/hls/<stream-key>.m3u8
```

#### 通过前端界面

1. 访问 `http://localhost`
2. 进入直播列表
3. 选择正在直播的房间观看

### 5.3 API接口

#### 健康检查

```bash
curl http://localhost:8081/api/health
```

#### 获取直播列表

```bash
curl http://localhost:8081/api/lives
```

#### 用户登录

```bash
curl -X POST http://localhost:8081/api/users/login \
     -H "Content-Type: application/json" \
     -d '{"username":"admin","password":"123456"}'
```

### 5.4 管理界面

| 服务 | 地址 | 账号/密码 |
|------|------|-----------|
| RabbitMQ管理 | http://localhost:15672 | guest / guest |
| Elasticsearch | http://localhost:9200 | 无认证 |

## 6. Docker部署

### 6.1 服务编排

**docker-compose.yml** 定义了以下服务：

1. **mysql** - MySQL数据库
2. **redis** - Redis缓存
3. **rabbitmq** - RabbitMQ消息队列
4. **elasticsearch** - Elasticsearch搜索引擎
5. **backend** - Spring Boot后端服务
6. **frontend** - Vue前端服务
7. **nginx-rtmp** - Nginx-RTMP流媒体服务

### 6.2 服务依赖关系

```
mysql ────────┐
redis ────────┤
rabbitmq ─────┼──> backend ──┬──> frontend
elasticsearch─┘              │
nginx-rtmp ──────────────────┘
```

### 6.3 数据持久化

| 服务 | 数据卷名称 | 用途 |
|------|------------|------|
| MySQL | mysql-data | 数据库数据 |
| Redis | redis-data | 缓存数据 |
| RabbitMQ | rabbitmq-data | 消息队列数据 |
| Elasticsearch | elasticsearch-data | 搜索索引 |

## 7. 常见问题

### 7.1 端口冲突

如果端口被占用，修改 `docker-compose.yml` 中的端口映射：

```yaml
ports:
  - "外部端口:内部端口"
```

### 7.2 数据库连接失败

1. 检查MySQL容器是否正常启动: `docker-compose ps mysql`
2. 查看MySQL日志: `docker-compose logs mysql`
3. 确认密码配置与docker-compose一致

### 7.3 推流失败

1. 检查nginx-rtmp容器状态: `docker-compose ps nginx-rtmp`
2. 确认推流地址格式正确
3. 检查防火墙是否阻止1935端口

### 7.4 直播延迟高

调整FFmpeg推流参数：

```bash
-preset ultrafast -tune zerolatency -g 30
```

### 7.5 后端服务启动失败

查看后端日志定位问题：

```bash
docker-compose logs -f backend
```

## 8. 本地开发流程

### 8.1 启动基础设施

```powershell
# 启动MySQL、Redis、RabbitMQ、Elasticsearch
docker-compose up -d mysql redis rabbitmq elasticsearch
```

### 8.2 启动后端

```powershell
# IDE中运行或命令行
cd backend
mvn spring-boot:run
```

### 8.3 启动前端

```powershell
cd frontend
npm install
npm run dev
```

### 8.4 启动流媒体服务

```powershell
docker-compose up -d nginx-rtmp
```

## 9. 监控与维护

### 9.1 查看资源使用

```bash
docker stats
```

### 9.2 清理无用镜像和容器

```bash
docker system prune -a
```

### 9.3 数据备份

```bash
# 备份MySQL
docker exec zhibo-mysql mysqldump -u root -p123456 zhibo > backup.sql

# 恢复MySQL
docker exec -i zhibo-mysql mysql -u root -p123456 zhibo < backup.sql
```

## 10. 项目结构

```
zhibo/
├── backend/                 # 后端项目
│   ├── src/
│   │   └── main/
│   │       ├── java/com/zhibo/backend/
│   │       └── resources/
│   │           ├── application.yml
│   │           └── sql/init.sql
│   ├── Dockerfile
│   └── pom.xml
├── frontend/                # 前端项目
│   ├── src/
│   │   ├── api/
│   │   ├── components/
│   │   └── router/
│   ├── Dockerfile
│   ├── nginx.conf
│   ├── vite.config.ts
│   └── package.json
├── stream/                  # HLS流文件
├── build-all.ps1            # 完整构建脚本
├── build-backend.ps1        # 后端构建脚本
├── build-frontend.ps1       # 前端构建脚本
├── start.ps1                # 启动脚本
├── docker-compose.yml       # Docker编排
├── nginx-rtmp.conf          # 流媒体配置
└── 项目配置与使用指南.md
```
